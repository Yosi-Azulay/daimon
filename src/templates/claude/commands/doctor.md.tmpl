---
name: bosun-doctor
description: Sanity-check the bosun config and environment. Exits 1 if anything is wrong.
bosun-version: {{bosun_version}}
generated-at: {{generated_at}}
---

Run: `bosun doctor`

Does not require the daemon. Returns `{ checks: [{name, ok, detail?}], ok: bool }`. On `ok: false`, report each failed check.
