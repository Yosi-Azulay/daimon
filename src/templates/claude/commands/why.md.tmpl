---
name: appman-why
description: Diagnose why an app is in error — returns the last status transition plus 5 preceding events.
appman-version: {{appman_version}}
generated-at: {{generated_at}}
---

Run: `appman why $1`

Returns one JSON object with the trigger event and surrounding context. Combine with `appman errors $1 --structured` for a complete picture.
