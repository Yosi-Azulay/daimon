{
  "surface": "daimon errors",
  "note": "Golden shape (key sets + types, never values). Regenerate with UPDATE_CONTRACT_SNAPSHOTS=1 and hand-review: keys may be ADDED, never removed or retyped.",
  "cases": {
    "compact": {
      "$array": {
        "code": "null|string",
        "col": "null|number",
        "file": "null|string",
        "level": "string",
        "line": "null|number",
        "message": "string"
      }
    },
    "full": {
      "$array": {
        "count": "number",
        "firstSeen": "number",
        "lastSeen": "number",
        "level": "string",
        "message": "string"
      }
    }
  }
}
