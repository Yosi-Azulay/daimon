{
  "surface": "GET /api/config",
  "note": "Golden shape (key sets + types, never values). Regenerate with UPDATE_CONTRACT_SNAPSHOTS=1 and hand-review: keys may be ADDED, never removed or retyped.",
  "cases": {
    "default": {
      "config": {
        "apiPort": "number",
        "apiToken": "null",
        "autoRestart": {
          "enabled": "boolean",
          "maxAttempts": "number",
          "windowMs": "number"
        },
        "autoStart": {
          "$array": "unknown"
        },
        "cascadeRestart": "boolean",
        "dashboard": {
          "density": "string",
          "theme": "string"
        },
        "depends": {},
        "doctor": {
          "autoFix": {
            "onInit": "boolean",
            "permitted": {
              "$array": "unknown"
            }
          }
        },
        "editor": {
          "scheme": "string"
        },
        "envFiles": {},
        "errorRetention": {
          "maxAgeMs": "number"
        },
        "frameworks": {
          "$array": "unknown"
        },
        "headless": "boolean",
        "healthProbe": {
          "enabled": "boolean",
          "intervalMs": "number",
          "path": "string",
          "timeoutMs": "number"
        },
        "history": {
          "enabled": "boolean",
          "path": "string",
          "retentionDays": "number"
        },
        "logs": {
          "dir": "string",
          "enabled": "boolean",
          "maxBytesPerFile": "number",
          "maxFiles": "number"
        },
        "metrics": {
          "enabled": "boolean"
        },
        "notifications": {
          "enabled": "boolean",
          "onError": "boolean",
          "onUnhealthy": "boolean",
          "tray": "boolean"
        },
        "output": {
          "format": "string",
          "ndjson": "boolean"
        },
        "overrides": {},
        "plugins": {
          "dir": "null"
        },
        "portRange": {
          "$array": "number"
        },
        "profiles": {
          "fullstack": {
            "$array": "string"
          }
        },
        "requestLog": {
          "enabled": "boolean",
          "portOffset": "number"
        },
        "searchRoots": {
          "$array": "unknown"
        },
        "staleDetect": {
          "enabled": "boolean",
          "silentMs": "number"
        },
        "tags": {},
        "webhooks": {
          "$array": "unknown"
        }
      },
      "etag": "string"
    }
  }
}
