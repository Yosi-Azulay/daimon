{
  "surface": "daimon daemon",
  "note": "Golden shape (key sets + types, never values). Regenerate with UPDATE_CONTRACT_SNAPSHOTS=1 and hand-review: keys may be ADDED, never removed or retyped.",
  "cases": {
    "status-stopped": {
      "running": "boolean"
    },
    "status-running": {
      "headless": "boolean",
      "pid": "number",
      "port": "number",
      "running": "boolean",
      "uptimeMs": "number",
      "version": "string"
    }
  }
}
